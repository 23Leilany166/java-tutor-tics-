package com.ejercicios.noresueltos;
import java.util.*;

public class esqueleto3_5{

    // Método para calcular el cubo (deben completar ellos)
    public static long cubo(long n) {
        // Aquí va la operación para obtener el cubo
        // return n * n * n;

        return 0; // temporal
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Declarar variable para leer números

        // Ciclo que se detiene cuando el usuario escribe -1

            // Volver a pedir otro número
 
        sc.close();
    }
}
